# Phase 2 corpus pipeline (legacy)
datasets

# Pragmatic alternative — see docs/wordfreq-recipe.md
wordfreq
simplemma

# Phase 3 web validation — see search_wild.py
# ddgs is the default provider (no API key); google-api-python-client is used by --provider google
ddgs
google-api-python-client

# Research UI
flask
